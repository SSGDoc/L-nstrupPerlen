<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

class CreateOrdersTable extends Migration
{
    /**
     * Run the migrations.
     *
     * @return void
     */
    public function up()
    {
        Schema::create('orders', function (Blueprint $table) {
            $table->bigIncrements('order_id');
            $table->date('date');
            $table->string('message');

            $table->bigInteger('statusId')->unsigned()->nullable();
            $table->bigInteger('customerId')->unsigned()->nullable();
            $table->foreign('statusId')->references('status_id')->on('statuses')->onDelete('cascade');
            $table->foreign('customerId')->references('customer_id')->on('customers')->onDelete('cascade');
        });
    }

    /**
     * Reverse the migrations.
     *
     * @return void
     */
    public function down()
    {
        Schema::dropIfExists('orders');
    }
}
